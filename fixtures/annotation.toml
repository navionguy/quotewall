[[scenario]]
name = "test annotations"

  [[scenario.table]]
    name = "annotations"

    [[scenario.table.row]]
      id = "<%= uuid() %>"
      note = "Note about the quote"
      created_at = "<%= now() %>"
      updated_at = "<%= now() %>"

    [[scenario.table.row]]
      id = "<%= uuid() %>"
      note = "First Timer!"
      created_at = "<%= now() %>"
      updated_at = "<%= now() %>"

